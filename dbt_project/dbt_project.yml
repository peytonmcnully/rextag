name: rextag
version: "1.0.0"
profile: rextag
config-version: 2

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  rextag:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
